# TeamSync v2.0 — Python dependencies

# Graph
langgraph>=0.2.28
langgraph-checkpoint-postgres>=2.0.0

# AI SDKs (Rakuten AI Gateway)
anthropic>=0.40.0          # kept for ai_service direct calls
langchain-anthropic>=0.3.0 # ChatAnthropic — used in graph nodes

# API server
fastapi>=0.115.6
uvicorn[standard]>=0.32.1
python-multipart>=0.0.17

# Database
sqlalchemy>=2.0.36
asyncpg>=0.30.0
aiosqlite>=0.20.0        # SQLite for tests only
greenlet>=3.0.0          # required by SQLAlchemy async engine
psycopg[binary]>=3.1.0   # psycopg3 with bundled libpq — required by langgraph-checkpoint-postgres

# Config & validation
pydantic>=2.10.3
pydantic-settings>=2.6.1

# HTTP client (JIRA, Gmail — Phase 2)
httpx>=0.28.1

# PDF text extraction (file upload feature)
pypdf>=4.0.0

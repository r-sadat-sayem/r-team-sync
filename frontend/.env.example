# Leave unset for local Vite development so requests use the dev-server proxy.
# Set this only when the frontend must call a different backend origin directly.
VITE_API_URL=
VITE_API_PROXY_TARGET=http://localhost:8000
